[project]
name = "pre-commit-vba-example"
version = "0.1.0"
description = "Example repository for pre-commit-vba"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]
